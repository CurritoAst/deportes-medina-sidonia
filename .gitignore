# Dependencias (no hay, pero por si acaso)
node_modules/

# Estado en tiempo de ejecución: lo crea el servidor y lo resiembra el navegador
data/estado.json

# Caché local y cola de accesos del servicio (estado en tiempo de ejecución)
acceso/cache-socios.json
acceso/cola-accesos.json
# Socios del lector simulado (UIDs/semillas locales, nunca al repo)
acceso/mock-socios.json

# Artefactos de desarrollo / pruebas
.playwright-mcp/
*.log
*.png
!icono-180.png

# Imagen de la Raspberry y copias (NUNCA subir: es enorme y contiene secretos)
*.img
torno_backup*

# Sistema
.DS_Store
Thumbs.db
