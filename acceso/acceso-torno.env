# ==========================================================================
# Configuración del servicio de acceso del torno.
# Se instala en /etc/acceso-torno.env. Tras editar, aplica los cambios con:
#     sudo systemctl restart acceso-torno
# ==========================================================================

# --- Web de gestión (tu despliegue en Render) ---
MSD_WEB=https://deportes-medina-sidonia.onrender.com

# --- Lectores del torno (convertidor HF5122) ---
MSD_HOST_ENTRADA=192.168.1.35
MSD_HOST_SALIDA=192.168.1.35
MSD_PUERTO_ENTRADA=8899
MSD_PUERTO_SALIDA=9999

# --- Relé de apertura (ajústalo a tu montaje; prueba con --test-rele) ---
# Backend GPIO: raspi-gpio (Pi OS antiguo) | pinctrl (Pi OS reciente) | sysfs | sim
MSD_GPIO=raspi-gpio
# Numeración de los pines: bcm | board (pin físico del header)
MSD_GPIO_NUM=bcm
# Polaridad: 1 = activo-bajo (las placas de relé Waveshare suelen serlo)
MSD_RELE_ACTIVO_BAJO=1
MSD_PIN_ENTRADA=26
MSD_PIN_SALIDA=20
# LEDs (opcionales): descomenta y pon los pines
#MSD_PIN_LED_VERDE=21
#MSD_PIN_LED_ROJO=16
MSD_PULSO_MS=600

# --- Arranque prudente ---
# Empieza en SOLO-ESCUCHA (no abre ni registra, solo deja huella en el journal)
# hasta confirmar la lectura; luego comenta esta línea y reinicia el servicio.
#MSD_SOLO_ESCUCHA=1
